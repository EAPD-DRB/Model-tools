interface:
  display_name: "Pull Handoff"
  short_description: "Pull CLEWs cases and maintain MUIOGO links"
  default_prompt: "Use $pull-handoff to pull Fiji and Philippines, safely install their current cases under each repository, and maintain relative MUIOGO DataStorage symlinks."
