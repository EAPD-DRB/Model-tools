interface:
  display_name: "Pull Handoff"
  short_description: "Pull and safely unzip current CLEWs cases"
  default_prompt: "Use $pull-handoff to pull the Fiji and Philippines repositories and safely install their documented current result-free cases in MUIOGO."
