interface:
  display_name: "Push Handoff"
  short_description: "Zip and push result-free Fiji and Philippines cases"
  default_prompt: "Use $push-handoff to zip the documented current Fiji and Philippines cases without solver results, verify them, and push the intended repository changes."
