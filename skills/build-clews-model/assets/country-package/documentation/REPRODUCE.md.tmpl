# Reproduce the {{COUNTRY}} raw model

Record exact checkout, environment, data-retrieval, patch, build, solve,
import, repair, parity, validation, packaging, and restore commands.

Use the pinned revisions and checksums in `../config/upstream_versions.json`.
